import * as React from 'react';
import JTurtle from './turtle-core';
interface Props {
    width: number;
    height: number;
    draw?: (turtle: JTurtle, width: number, height: number) => ((i: number) => boolean | void) | void;
    animated?: boolean;
    pixelated?: boolean;
    autostroke?: boolean;
    style?: React.CSSProperties;
}
declare const Turtle: React.FC<Props>;
export default Turtle;
